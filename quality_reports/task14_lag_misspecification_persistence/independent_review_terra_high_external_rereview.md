## Verdict: REPAIR

**Data:** 2026-07-12  
**Escopo:** re-revisão adversarial, somente leitura, da implementação corrigida da Task 14. Li o parecer externo anterior, o protocolo atualizado, os cinco arquivos em `R/`, runner, smoke test e Rmd. Não executei `--mode full` e não alterei scripts, dados, manuscrito ou resultados.

**Síntese.** F-01 está resolvido no DGP: a inicialização, a transição de estado para ADL(1/2/3), a covariância de Lyapunov, a determinística e o manifesto são coerentes e passaram preflight/smoke. F-02 e F-03 não estão resolvidos. Em particular, a validação final ainda é apenas registrada, e não impede nem a promoção de outputs nem a renderização; os denominadores efetivos de ACF e dos shifts continuam ausentes.

**Evidência executada.** O preflight passou as 11 invariantes nas 27 células (raio máximo `0.987669`; resíduo máximo de Lyapunov `7.82e-14`). `smoke_test.R` passou integralmente: DGPs ADL(1/2/3) bitwise determinísticos, painel balanceado, equivalência do coeficiente com `fixest`, retenção de regras/métricas, round trip CSV, seleção-base e recusa de `--mode full` sem `--approved`. `git diff --check` também passou. Não foi necessária renderização temporária para estabelecer os bloqueios abaixo.

**F-01 — resolvido — severidade: nenhuma pendência bloqueadora.** `lmp_transition_components()` representa o estado como `(D_t, Y_t, Z_t, D_{t-1}, Y_{t-1}, ...)`, com transição contemporânea consistente: `D_t` usa `Z_{t-1}`; `Y_t` inclui o CET, lags de `D` e `Y`; e `Z_t` é gerado após ambos. A matriz de inovação contém corretamente os choques contemporâneos propagados por CET, `delta_D` e `delta_Y`. O mapeamento em `lmp_simulate_panel()` coloca exatamente esse estado em `t=p` e seus lags em `p-1,...,1`; portanto cobre ADL(1), ADL(2) e ADL(3), sem trocar lags. Arquivos/linhas: `R/config.R:97-143`; `R/dgp.R:24-59`.

A covariância resolve `Sigma = A Sigma A' + B B'`, é simetrizada, tem resíduo testado contra `1e-10`, e o draw usa o fator de Cholesky com a orientação correta. A média condicional inclui os três efeitos unitários e seus efeitos contemporâneos induzidos. O DGP recusa resíduo acima da tolerância antes dos draws. Arquivos/linhas: `R/config.R:151-198`; `R/dgp.R:15-21`. A validação/preflight cobre cada célula, o smoke testa a célula de maior raio, e o runner grava `initialization_manifest.csv` com raio, método, burn-in pós-draw, resíduo e indicador de aprovação. Arquivos/linhas: `R/config.R:200-276`; `smoke_test.R:25-55`; `run_lag_misspecification_persistence.R:171-175`. O seed continua função apenas de cenário e repetição e o smoke confirma determinismo. Arquivos/linhas: `R/config.R:279-285`; `smoke_test.R:64-100`.

**F-02 — não resolvido — severidade: alta, bloqueia a bateria completa.** `lmp_output_validation()` devolve uma tabela de checks, incluindo a inicialização estacionária, mas não interrompe quando algum `passed` é falso. `lmp_write_outputs()` calcula a tabela, grava `validation.csv` e prossegue para gravar todos os outputs e o manifesto de execução. Em seguida, `lmp_main()` chama a renderização sem exigir `all(outputs$validation$passed)`. Assim, um resultado inválido pode terminar com CSVs promovidos, `run_manifest.csv` de conclusão e PDF renderizado. Arquivos/linhas: `R/metrics.R:131-180`; `run_lag_misspecification_persistence.R:147-211`; `run_lag_misspecification_persistence.R:281-285`.

O smoke apenas mostra que um input válido passa a validação; ele não injeta um output inválido para provar que o runner recusa antes de escrever/renderizar. Arquivos/linhas: `smoke_test.R:126-129`. Reparo requerido: gravar o diagnóstico de validação de forma atômica e interromper imediatamente se algum check falhar, antes de escrever artefatos finais, manifesto de conclusão ou renderizar; adicionar ao smoke uma entrada sintética inválida que comprove a recusa e a ausência de PDF.

**F-03 — não resolvido — severidade: média.** Para ACF, `mean_residual_acf1` e `mcse_residual_acf1` descartam valores não finitos, mas o output salva apenas `n_success`; não salva `n_acf_finite`, que é o denominador efetivo das duas estatísticas. Arquivos/linhas: `R/metrics.R:7-43`; `R/metrics.R:102-112`.

Para os shifts, o resumo parte apenas das linhas `Z_lag` bem-sucedidas. Cada shift, porém, exige um par distinto: `no_Z` + `Z_lag`, ou `no_Z` + `Z_contemporaneous`. Os valores finitos são corretamente filtrados para a média/MCSE, mas `n_success = .N` é o número de fits `Z_lag`, não o número de pares válidos; tampouco há `n_attempted`, `n_failed`, `n_delta_lag_pairs` ou `n_delta_contemporaneous_pairs`. Arquivos/linhas: `R/simulation.R:191-205`; `R/metrics.R:76-100`. O relatório também agrega essas médias sem expor os denominadores. Arquivos/linhas: `lag_misspecification_persistence_report.Rmd:154-183`.

Reparo requerido: salvar os denominadores efetivos e calcular cada média/MCSE com seu conjunto pareado explícito (`n_acf_finite`, `n_delta_lag_pairs`, `n_delta_contemporaneous_pairs`, além de tentativas/falhas pertinentes); documentá-los no protocolo/outputs e adicionar ao smoke uma falha parcial sintética que diferencie fit bem-sucedido de par válido.

**Handoff.** Não executar `--mode full --approved` nem usar resultados no manuscrito até que F-02 e F-03 sejam reparados, o preflight/smoke sejam repetidos e uma nova revisão independente emita `## Verdict: PASS`.
